# SQLite-WebClient

[![License](https://img.shields.io/badge/License-MIT-green.svg?style=flat-square)](LICENSE)

ブラウザ上で完全に動作するSQLiteクライアント。WebAssembly技術により、サーバーサイドの設定なしにブラウザ内でSQLiteデータベースの操作が可能です。

**ライブデモ**: [https://nojaja.github.io/SQLite-WebClient/](https://nojaja.github.io/SQLite-WebClient/)

## プロジェクト概要

SQLite-WebClientは、[@sqlite.org/sqlite-wasm](https://www.npmjs.com/package/@sqlite.org/sqlite-wasm)を使用してブラウザ上でSQLiteデータベースを操作できるWebアプリケーションです。すべての処理がクライアントサイドで完結し、プライバシーとセキュリティに配慮した設計となっています。

## プロジェクト構造

```
SQLite-WebClient/
├── src/
│   ├── js/
│   │   ├── index.js           # エントリポイント・UIロジック
│   │   └── SQLiteManager.js   # SQLite WASM管理クラス
│   ├── html/
│   │   └── index.html         # メインHTMLテンプレート
│   ├── css/                   # スタイルシート (オプション)
│   └── assets/                # 静的アセット (オプション)
├── dist/                      # ビルド出力ディレクトリ
├── package.json               # プロジェクト設定
├── webpack.config.js          # Webpack設定
├── LICENSE                    # MITライセンス
└── README.md                  # このファイル
```

## 技術スタック

### コア依存関係
- **[@sqlite.org/sqlite-wasm](https://www.npmjs.com/package/@sqlite.org/sqlite-wasm)** (^3.45.3-build3) - SQLite公式WebAssembly実装
- **[dbgate-query-splitter](https://www.npmjs.com/package/dbgate-query-splitter)** (^4.11.4) - SQLクエリの分割処理
- **path-browserify** (^1.0.1) - ブラウザ環境でのパス処理
- **process** (^0.11.10) - Node.js互換プロセスポリフィル

### 開発環境
- **Webpack 5** (^5.89.0) - モジュールバンドラ
- **webpack-dev-server** (^4.15.2) - 開発サーバー
- **copy-webpack-plugin** (^11.0.0) - WASMファイルコピー
- **html-webpack-plugin** (^4.5.2) - HTMLファイル生成
- **cross-env** (^7.0.3) - クロスプラットフォーム環境変数設定

## 主な機能

### ✅ 実装済み機能

1. **SQLクエリ実行**
   - SQLiteバージョン情報の取得
   - CREATE, INSERT, SELECT等の基本SQL操作
   - 複数のSQL文の連続実行（セミコロン区切り）
   - クエリ結果のリアルタイム表示

2. **データベース管理**
   - ブラウザ内でのインメモリデータベース作成
   - データベースのエクスポート（.dbファイルとしてダウンロード）
   - データベースのインポート（ローカル.dbファイルを読み込み）

3. **SQLiteManager API**
   - データベース初期化・セットアップ
   - プリペアドステートメントのオーバーライド
   - クエリの分割実行（dbgate-query-splitter統合）
   - スキーマ情報の取得（テーブル、ビュー、インデックス、トリガー）
   - テーブル構造の取得（PRAGMA table_info）

4. **クロスプラットフォーム対応**
   - ブラウザ環境での実行
   - Node.js環境での実行（条件付き）
   - VFS（Virtual File System）によるファイル操作

## セットアップ

### 前提条件
- Node.js v14以降
- npm v6以降
- モダンブラウザ（WebAssembly対応）

### インストール手順

1. **リポジトリのクローン**
   ```bash
   git clone https://github.com/nojaja/SQLite-WebClient.git
   cd SQLite-WebClient
   ```

2. **依存関係のインストール**
   ```bash
   npm install
   ```

3. **開発サーバーの起動**
   ```bash
   npm start
   ```

4. **ブラウザでアクセス**
   ```
   http://localhost:8080
   ```

## 使用方法

### Webアプリケーション使用

#### 開発環境での実行

```bash
# 開発サーバー起動（ホットリロード有効）
npm start

# ブラウザで http://localhost:8080 にアクセス
```

#### SQLクエリの実行

1. テキストエリアにSQLクエリを入力
   ```sql
   SELECT sqlite_version();
   CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT);
   INSERT INTO users VALUES (1, 'Alice');
   SELECT * FROM users;
   ```

2. **「execute」ボタン**をクリックして実行

3. 実行結果が下部の出力エリアに表示されます
   - `sql >` - 実行されたSQL文
   - `result.columns>` - カラム名
   - `result.values>` - 取得されたデータ

#### データベースのエクスポート/インポート

- **エクスポート**: 「データベースのexport」ボタンをクリック → `Untitled.db`としてダウンロード
- **インポート**: 「データベースのimport」ボタンをクリック → ローカルの.dbファイルを選択

### ライブラリとしての使用

#### SQLiteManagerクラスの初期化

```javascript
import SQLiteManager from './SQLiteManager.js';

// ブラウザ環境での初期化
const sqliteManager = await SQLiteManager.initialize(null, {
  print: console.log,
  printErr: console.error
});

// データを持つデータベースをインポート
const data = new Uint8Array(arrayBuffer); // 既存の.dbファイルデータ
const sqliteManager = await SQLiteManager.initialize(data, options);
```

#### API リファレンス

| メソッド | パラメータ | 戻り値 | 説明 |
|---------|-----------|--------|------|
| `SQLiteManager.initialize(data, options)` | `data`: Uint8Array (オプション)<br>`options`: Object | `Promise<SQLiteManager>` | SQLiteインスタンスを初期化。dataでDB内容を指定可能 |
| `db.exec(sql, bind)` | `sql`: string<br>`bind`: Object (オプション) | `Array<{columns, values}>` | SQLを実行し、結果を取得 |
| `splitStatements(sql)` | `sql`: string | `Array<string>` | SQL文をセミコロンで分割 |
| `executeQuery(query)` | `query`: string | `Array<{success, results, columns}>` | クエリを実行し、整形された結果を返す |
| `getDatabaseSchema()` | なし | `{tables, views, indexes, triggers}` | データベース全体のスキーマ情報を取得 |
| `getTableStructure(tableName)` | `tableName`: string | `Array<Object>` | 指定テーブルの列情報を取得 |
| `export()` | なし | `Uint8Array` | データベース全体をバイナリでエクスポート |
| `import(contents)` | `contents`: Uint8Array | `Promise<void>` | データベースをインポート（既存DBはクローズ） |
| `close()` | なし | `void` | データベース接続をクローズ |

#### 使用例

```javascript
// クエリ実行
const results = sqliteManager.db.exec('SELECT * FROM users');
console.log(results[0].columns); // ['id', 'name']
console.log(results[0].values);  // [[1, 'Alice'], [2, 'Bob']]

// スキーマ取得
const schema = sqliteManager.getDatabaseSchema();
console.log(schema.tables); // ['users', 'posts', ...]

// テーブル構造取得
const structure = sqliteManager.getTableStructure('users');
// [{cid: 0, name: 'id', type: 'INTEGER', ...}, ...]

// データベースのエクスポート
const dbData = sqliteManager.export();
// Save to file or transfer to server

// クローズ
sqliteManager.close();
```

### プロダクションビルド

```bash
# 本番用ビルド（dist/ディレクトリに出力）
npm run build

# ビルド結果の確認
ls dist/
# main.bundle.js, index.html, sqlite3.wasm, etc.
```

### 開発時のウォッチモード

```bash
# ファイル変更を監視して自動再ビルド
npm run dev
```

## 技術的な詳細

### WebAssemblyの初期化

SQLite-WebClientは[@sqlite.org/sqlite-wasm](https://sqlite.org/wasm)の公式実装を使用しています。初期化プロセス：

1. WASMモジュールのロード
2. 環境検出（ブラウザ/Node.js）
3. VFS（仮想ファイルシステム）のセットアップ
4. データベースインスタンスの作成

### カスタムメソッドのオーバーライド

SQLiteManagerクラスは、元のSQLite WASM APIを拡張し、より使いやすいインターフェースを提供：

- **`db.prepare`のオーバーライド**: `getAsObject()`メソッドを追加し、行データをオブジェクトとして取得可能に
- **`db.exec`のオーバーライド**: 結果を`{columns, values}`形式で統一的に返却
- **バインドパラメータのフィルタリング**: 不要なパラメータを自動除外

### クエリ分割処理

`dbgate-query-splitter`を使用して、複数のSQL文を正確に分割：

```javascript
const statements = sqliteManager.splitStatements(`
  CREATE TABLE test (id INT);
  INSERT INTO test VALUES (1);
  SELECT * FROM test;
`);
// ['CREATE TABLE test (id INT)', 'INSERT INTO test VALUES (1)', 'SELECT * FROM test']
```

## 現在の状態

### ✅ 実装済み
- SQLite WASM初期化
- 基本的なSQL操作（CRUD）
- データベースのインポート/エクスポート
- スキーマ情報の取得
- クエリ分割と連続実行
- クロスプラットフォーム対応（ブラウザ/Node.js）

### 🔧 今後の拡張
- カスタム関数の登録機能（コード内にコメントアウトされた実装あり）
- より高度なUI（シンタックスハイライト、オートコンプリート）
- クエリ履歴の保存
- 複数データベースの同時管理

## パフォーマンスと制限事項

### 利点
- ✅ **完全クライアントサイド**: すべての処理がブラウザ内で完結
- ✅ **サーバー不要**: バックエンドインフラが不要
- ✅ **プライバシー保護**: データがサーバーに送信されない
- ✅ **オフライン動作**: ネットワーク接続不要
- ✅ **軽量**: 依存関係が少なくバンドルサイズが小さい

### 制限事項
- ⚠️ **ブラウザメモリ依存**: 大規模データセット（数百MB以上）の処理には不向き
- ⚠️ **WebAssembly必須**: 古いブラウザでは動作しない（IE11等）
- ⚠️ **永続化制限**: ページをリロードするとインメモリデータは消失（エクスポートが必要）
- ⚠️ **ファイルシステムアクセス制限**: ブラウザのセキュリティ制約によりファイル操作に制限あり

### 推奨環境
- Chrome/Edge 89+
- Firefox 89+
- Safari 14.1+

## ライセンス

MIT License

Copyright (c) 2021 nojaja

詳細は[LICENSE](LICENSE)ファイルを参照してください。

## 作者

**nojaja**
- GitHub: [@nojaja](https://github.com/nojaja)
- Email: free.riccia@gmail.com

## 関連リンク

- [公式ライブデモ](https://nojaja.github.io/SQLite-WebClient/)
- [SQLite WASM公式ドキュメント](https://sqlite.org/wasm/doc/trunk/index.md)
- [リポジトリ](https://github.com/nojaja/SQLite-WebClient)
